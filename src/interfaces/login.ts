import { Navigation, Route } from "./home";


export interface PropsLoginScreen {
 
    navigation: Navigation,
    route: Route,

} 
export interface PropsRegisterScreen {
 
    navigation: Navigation,
    route?: Route,

} 